<QuestionForm
  @titleError={{this.titleError}}
  @tagsError={{this.tagsError}}
  @errors={{this.errors}}
  @model={{@model}}
  @tagList={{this.questionTags}}
  @buttonTitle={{"Update"}}
  @updateQuestion={{this.updateQuestion}}
/>