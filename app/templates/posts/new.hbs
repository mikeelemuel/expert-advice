<QuestionForm
  @titleError={{this.titleError}}
  @tagsError={{this.tagsError}}
  @errors={{this.errors}}
  @model={{@model}}
  @tagList={{this.tagList}}
  @buttonTitle={{"POST A QUESTION"}}
  @updateQuestion={{this.createQuestion}}
/>